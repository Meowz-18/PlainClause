// Benchmark entry point — delegates to bench.js (plain JS for zero-dependency execution).
// Run via: npm run bench
// The JS implementation lives in scripts/bench.js and uses only Node built-ins + fixture files.
export {};
